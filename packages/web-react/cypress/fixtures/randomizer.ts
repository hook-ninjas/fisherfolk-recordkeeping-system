// import { RegistrationType, Salutation, CivilStatus, EducationalBackground, Gender, Nationality, SourceOfIncome } from '@prisma/client';
// import { barangays } from '../../src/Components/Forms/iloilo-city-brgys.json';

// const replaceUnderscore = (input: string) => input.replace('_', ' ');

// export const randomizer = () => {
//   const randomNumber = Math.floor(Math.random() * 10);
//   return randomNumber;
// };

// export const randomInt = (max: number, min = 0) => {
//   min = Math.ceil(min);
//   max = Math.floor(max);
//   return Math.floor(Math.random() * (max - min) + min);
// };

// export const randomRegistrationType = () => {
//   const registrationType = Object.values(RegistrationType);
//   return replaceUnderscore(registrationType[Math.floor(Math.random() * registrationType.length)]);
// };

// export const randomSalutation = () => {
//   const salutation = Object.values(Salutation);
//   return salutation[Math.floor(Math.random() * salutation.length)];
// };

// export const randomBarangay = () => {
//   const barangay = Object.values(barangays);
//   return barangay[Math.floor(Math.random() * barangay.length)];
// };

// export const randomCivilStatus = () => {
//   const civilStatus = Object.values(CivilStatus);
//   return replaceUnderscore(civilStatus[Math.floor(Math.random() * civilStatus.length)]);
// };

// export const randomEducationalBackground = () => {
//   const educationalBackground = Object.values(EducationalBackground);
//   return replaceUnderscore(educationalBackground[Math.floor(Math.random() * educationalBackground.length)]);
// };

// export const randomGender = () => {
//   const gender = Object.values(Gender);
//   return gender[Math.floor(Math.random() * gender.length)];
// };

// export const randomNationality = () => {
//   const nationality = Object.values(Nationality);
//   return nationality[Math.floor(Math.random() * nationality.length)];
// };

// export const randomSourceOfIncome = () => {
//   const sourceOfIncome = Object.values(SourceOfIncome);
//   return replaceUnderscore(sourceOfIncome[Math.floor(Math.random() * sourceOfIncome.length)]);
// };
